@page
@model RacingLeagueManager.Pages.LeagueDriver.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h2>Join league</h2>

<h4>@Html.DisplayFor(model => model.LeagueDriver.League.Name)</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @*<label asp-for="LeagueDriver.LeagueId" class="control-label"></label>
                    <select asp-for="LeagueDriver.LeagueId" class ="form-control" asp-items="ViewBag.LeagueId"></select>*@
                <input asp-for="LeagueDriver.LeagueId" type="hidden" />
            </div>
            @*<div class="form-group">
                    <label asp-for="LeagueDriver.DriverId" class="control-label"></label>
                    <select asp-for="LeagueDriver.DriverId" class ="form-control" asp-items="ViewBag.DriverId"></select>
                </div>*@
            @*<div class="form-group">
                <label asp-for="LeagueDriver.RaceNumber" class="control-label"></label>
                <input asp-for="LeagueDriver.RaceNumber" class="form-control" />
                <span asp-validation-for="LeagueDriver.RaceNumber" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="LeagueDriver.PreQualifiedTime" class="control-label"></label>
                <input asp-for="LeagueDriver.PreQualifiedTime" class="form-control" />
                <span asp-validation-for="LeagueDriver.PreQualifiedTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
