@page
@model RacingLeagueManager.Pages.SeriesDriver.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@*<p>
    <a asp-page="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.SeriesDriver[0].LeagueDriver.Driver.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeriesDriver[0].LeagueDriver.PreQualifiedTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeriesDriver[0].Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SeriesDriver) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LeagueDriver.Driver.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LeagueDriver.PreQualifiedTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            
            
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>*@
        </tr>
}
    </tbody>
</table>
