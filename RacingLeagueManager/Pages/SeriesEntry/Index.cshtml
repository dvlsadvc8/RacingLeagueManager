@page
@model RacingLeagueManager.Pages.SeriesEntry.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SeriesEntry[0].Series)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeriesEntry[0].Car)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeriesEntry[0].RaceNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SeriesEntry) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Series.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Car.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RaceNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
